'use client';

import React from 'react';

/**
 * Presentational profile portrait — kept as a reusable component for future use.
 * The previous cross-section scroll-driven Hero→About arc / 360° flip was
 * removed; this file now exists only as a stable export target so existing
 * imports don't break. See Hero.tsx for the actual static rendering.
 */
export const PinnedProfileCard: React.FC<{ imageUrl?: string | null }> = () => {
  return null;
};

export default PinnedProfileCard;